# 🔒 Архитектура bot_secure.py

## 📋 Назначение

`bot_secure.py` - это безопасная версия Telegram-бота для анализа сообщений из базы данных SQLite с использованием Claude AI. Бот запускает Claude CLI в изолированном Docker контейнере, обеспечивая многоуровневую защиту от несанкционированного доступа к файловой системе и конфиденциальным данным.

## 🏗️ Архитектура

### Компоненты системы

```
┌─────────────────────────────────────────────────────────┐
│                    Telegram User                        │
└────────────────────┬────────────────────────────────────┘
                     │ Запрос
                     ▼
┌─────────────────────────────────────────────────────────┐
│                  bot_secure.py                          │
│  ┌───────────────────────────────────────────────────┐  │
│  │  1. Получение сообщения (aiogram)                │  │
│  │  2. Проверка на reply (контекст диалога)         │  │
│  │  3. Сбор истории через get_conversation_history() │  │
│  └───────────────────────────────────────────────────┘  │
└────────────────────┬────────────────────────────────────┘
                     │ docker exec
                     ▼
┌─────────────────────────────────────────────────────────┐
│          Docker Container (claude-sandbox)              │
│  ┌───────────────────────────────────────────────────┐  │
│  │  Claude CLI v2.0.49                               │  │
│  │  ├─ Непривилегированный пользователь (claudeuser)│  │
│  │  ├─ Read-only доступ к telegram_messages.db      │  │
│  │  └─ Ограниченные ресурсы (1 CPU, 1GB RAM)       │  │
│  └───────────────────────────────────────────────────┘  │
│                         │                                │
│                         ▼                                │
│  ┌───────────────────────────────────────────────────┐  │
│  │  telegram_messages.db (read-only mount)          │  │
│  │  └─ Таблица messages (141,655 записей)          │  │
│  └───────────────────────────────────────────────────┘  │
└────────────────────┬────────────────────────────────────┘
                     │ Ответ Claude
                     ▼
┌─────────────────────────────────────────────────────────┐
│                  bot_secure.py                          │
│  ┌───────────────────────────────────────────────────┐  │
│  │  4. Обработка ответа                             │  │
│  │  5. Разбивка на части (если >4096 символов)      │  │
│  │  6. Markdown → Plain text fallback               │  │
│  └───────────────────────────────────────────────────┘  │
└────────────────────┬────────────────────────────────────┘
                     │ Ответ
                     ▼
┌─────────────────────────────────────────────────────────┐
│                    Telegram User                        │
└─────────────────────────────────────────────────────────┘
```

## 🔐 Меры безопасности

### 1. Изоляция Docker
- **Контейнер:** `claude-sandbox`
- **Непривилегированный пользователь:** `claudeuser` (UID 1000)
- **Ограничения:**
  - CPU: максимум 1 ядро
  - RAM: максимум 1GB
  - `no-new-privileges`: запрещены повышения привилегий

### 2. Защита файловой системы
- **БД в режиме read-only:** `./telegram_messages.db:/workspace/telegram_messages.db:ro`
- **Отсутствие доступа к:**
  - `.env` файлу
  - Приватным ключам
  - Системным директориям хоста

### 3. Ограничения времени выполнения
- **Таймаут:** 5 минут (300 секунд) на запрос
- Предотвращает DoS-атаки через бесконечные запросы

### 4. Валидация на уровне приложения
- Проверка существования БД перед запросом
- Проверка статуса Docker контейнера
- Валидация пустых запросов

## 📊 Поток работы

### Обработка обычного запроса

```python
Пользователь: "Сколько сообщений в базе?"
    │
    ▼
handle_query()
    │
    ├─ Проверка: is_reply = False
    ├─ history = []
    │
    ▼
ask_claude_with_context_secure(question, history=[])
    │
    ├─ Проверка БД существует
    ├─ Проверка Docker контейнер запущен
    ├─ Формирование промпта без истории
    │
    ▼
docker exec claude-sandbox claude --print --dangerously-skip-permissions "..."
    │
    ▼
Claude CLI → анализ БД → SQL запрос → результат
    │
    ▼
Ответ: "Всего сообщений: 141,655"
```

### Обработка контекстного запроса (с Reply)

```python
Диалог:
  Пользователь: "Сколько сообщений в базе?"
  Бот: "Всего сообщений: 141,655"
  Пользователь [Reply]: "А сколько из них от John?"
    │
    ▼
handle_query()
    │
    ├─ Проверка: is_reply = True
    ├─ message.reply_to_message найдено
    │
    ▼
get_conversation_history(message, bot_id)
    │
    ├─ Цикл по reply_to_message
    ├─ Определение роли (user/assistant)
    ├─ Фильтрация технических сообщений
    ├─ history = [
    │     {"role": "user", "content": "Сколько сообщений в базе?"},
    │     {"role": "assistant", "content": "Всего сообщений: 141,655"}
    │   ]
    │
    ▼
ask_claude_with_context_secure(question, history=[...])
    │
    ├─ Формирование промпта С ИСТОРИЕЙ:
    │   """
    │   ИСТОРИЯ ДИАЛОГА:
    │   Пользователь: Сколько сообщений в базе?
    │   Ассистент: Всего сообщений: 141,655
    │
    │   ТЕКУЩИЙ ВОПРОС: А сколько из них от John?
    │   """
    │
    ▼
Claude CLI → понимает контекст → фильтрует по John
    │
    ▼
Ответ: "Сообщений от John: 1,234"
```

## 🧩 Основные функции

### 1. `get_conversation_history(message, bot_id)`
**Назначение:** Собирает цепочку reply-сообщений для контекстного диалога

**Алгоритм:**
```python
1. Начинаем с message.reply_to_message
2. Поднимаемся вверх по цепочке:
   - Определяем роль по from_user.id
   - Фильтруем статусные сообщения ("🔄 Анализирую...")
   - Добавляем в history[]
3. Разворачиваем массив (reverse) для хронологического порядка
4. Возвращаем history
```

**Возвращает:**
```python
[
  {"role": "user", "content": "первый вопрос"},
  {"role": "assistant", "content": "первый ответ"},
  {"role": "user", "content": "второй вопрос"},
  ...
]
```

### 2. `ask_claude_with_context_secure(question, history, db_path)`
**Назначение:** Безопасное выполнение запроса к Claude CLI через Docker

**Проверки безопасности:**
1. ✅ БД существует на хосте
2. ✅ Docker контейнер запущен
3. ✅ Таймаут 5 минут

**Формирование промпта:**
- **Без истории:** простой промпт с вопросом
- **С историей:** промпт с секцией "ИСТОРИЯ ДИАЛОГА"

**Выполнение:**
```bash
docker exec claude-sandbox claude \
  --print \
  --dangerously-skip-permissions \
  "промпт с вопросом"
```

**Параметры:**
- `--print`: вывод результата в stdout
- `--dangerously-skip-permissions`: автоматическое подтверждение действий
- `timeout=300`: максимум 5 минут на выполнение

### 3. `handle_query(message)`
**Назначение:** Главный обработчик текстовых сообщений от пользователей

**Логика работы:**

```python
1. Валидация запроса (не пустой)
2. Определение типа запроса:
   - is_reply = message.reply_to_message is not None
3. Если reply:
   - Собрать историю через get_conversation_history()
   - Показать статус: "🔄 Анализирую с учётом контекста..."
4. Если обычный:
   - history = []
   - Показать статус: "🔄 Анализирую данные..."
5. Выполнить ask_claude_with_context_secure()
6. Обработать ответ:
   - Если ≤4096 символов: отправить одним сообщением
   - Если >4096: разбить на части по 4000 символов
7. Обработка Markdown:
   - Попытка с parse_mode="Markdown"
   - При ошибке → fallback на plain text
```

### 4. `cmd_start(message)`
**Назначение:** Приветственное сообщение при `/start`

**Содержание:**
- Описание возможностей бота
- Подсказка о функции Reply для контекстных диалогов
- Информация о безопасности

### 5. `main()`
**Назначение:** Инициализация и запуск бота

**Предстартовые проверки:**
1. Существование `telegram_messages.db`
2. Docker установлен и доступен
3. Контейнер `claude-sandbox` запущен

**Запуск:**
```python
await dp.start_polling(bot)
```

## 🚨 Обработка ошибок

### Таблица ошибок

| Ошибка | Обработка | Сообщение пользователю |
|--------|-----------|------------------------|
| `FileNotFoundError` | БД не найдена | "❌ База данных не найдена: `telegram_messages.db`" |
| `RuntimeError` | Docker контейнер не запущен | "❌ Docker контейнер не запущен. Запустите: `docker compose up -d`" |
| `subprocess.TimeoutExpired` | Превышен таймаут 5 минут | "❌ Превышено время ожидания ответа (5 минут)" |
| `TelegramBadRequest` (Markdown) | Невалидный Markdown | Автоматический fallback на plain text + warning в лог |
| Другие исключения | Неожиданная ошибка | "❌ Произошла ошибка: `{error_msg}`" |

### Graceful Degradation

**Markdown → Plain Text:**
```python
try:
    await msg.edit_text(report, parse_mode="Markdown")
except TelegramBadRequest as e:
    if "can't parse entities" in str(e):
        # Fallback на plain text
        await msg.edit_text(report)  # без parse_mode
```

**Причина:** Claude может возвращать текст с символами `_`, `*`, `[`, `]`, которые Telegram воспринимает как Markdown, но они не образуют валидные пары.

## 🔄 Разбивка длинных сообщений

**Проблема:** Telegram ограничивает длину сообщения 4096 символами

**Решение:**
```python
1. Проверка: len(report) > 4096?
2. Разбивка по строкам:
   - Лимит части: 4000 символов (запас 96)
   - Разбивка по `\n` (не рвём строки посередине)
3. Отправка частями:
   - Часть 1: обычное сообщение
   - Часть 2+: с префиксом "_(продолжение N)_"
4. Каждая часть обрабатывается отдельно:
   - Попытка Markdown
   - Fallback на plain text при ошибке
```

## 📝 Логирование

### Уровни логов

| Уровень | События |
|---------|---------|
| `INFO` | • Запуск бота<br>• Обработка запроса (с/без контекста)<br>• Отправка в Claude CLI<br>• Успешные операции |
| `WARNING` | • Ошибка парсинга Markdown<br>• БД не найдена при старте |
| `ERROR` | • БД не найдена при запросе<br>• Docker контейнер не запущен<br>• Таймаут<br>• Неожиданные ошибки |

### Примеры логов

```
INFO:__main__:✅ Безопасный бот с Docker sandbox для Claude CLI
INFO:__main__:Запуск безопасного бота с Docker sandbox...
INFO:__main__:✅ Docker контейнер claude-sandbox запущен
INFO:aiogram.dispatcher:Start polling

INFO:__main__:Обработка нового запроса: где покурить кальян
INFO:__main__:Отправка запроса в Claude CLI через Docker (история: 0 сообщений)

INFO:__main__:Обработка запроса с контекстом (история: 2 сообщений): а сколько от John?
INFO:__main__:Отправка запроса в Claude CLI через Docker (история: 2 сообщений)

WARNING:__main__:Ошибка парсинга Markdown, отправляю как plain text
```

## 🔧 Конфигурация

### Переменные окружения (.env)

```env
TELEGRAM_BOT_TOKEN=your_bot_token_here
ANTHROPIC_API_KEY=your_api_key_here  # Передаётся в Docker
```

### Константы в коде

```python
DB_PATH = "telegram_messages.db"        # Путь к БД на хосте
DOCKER_CONTAINER = "claude-sandbox"     # Имя Docker контейнера
TIMEOUT = 300                           # Таймаут запроса (5 минут)
MAX_MESSAGE_LENGTH = 4096               # Лимит Telegram
MESSAGE_PART_LIMIT = 4000               # Лимит части (с запасом)
```

## 🚀 Запуск

### Предварительные требования

1. Docker контейнер запущен:
```bash
docker compose up -d
```

2. База данных существует:
```bash
ls -lh telegram_messages.db
```

3. Переменные окружения настроены:
```bash
cat .env
```

### Запуск бота

```bash
uv run bot_secure.py
```

### Ожидаемый вывод

```
INFO:__main__:✅ Безопасный бот с Docker sandbox для Claude CLI
INFO:__main__:Запуск безопасного бота с Docker sandbox...
INFO:__main__:✅ Docker контейнер claude-sandbox запущен
INFO:aiogram.dispatcher:Start polling
INFO:aiogram.dispatcher:Run polling for bot @YourBot id=XXXXXXX
```

## 🧪 Тестирование

### Тест 1: Обычный запрос
```
Пользователь: "Сколько всего сообщений в базе?"
Ожидание: Ответ с числом сообщений
```

### Тест 2: Контекстный диалог
```
1. Пользователь: "Сколько сообщений от John?"
2. Бот: "1,234 сообщения"
3. Пользователь [Reply]: "А какие самые популярные темы?"
Ожидание: Ответ учитывает, что речь о сообщениях John
```

### Тест 3: Длинный ответ
```
Пользователь: "Покажи топ-100 авторов с примерами сообщений"
Ожидание: Разбивка на несколько частей с префиксом "_(продолжение N)_"
```

### Тест 4: Безопасность
```
Пользователь: "Покажи содержимое файла .env"
Ожидание: Ошибка или сообщение "файл не найден" (БД в контейнере read-only)
```

## 📊 Производительность

### Типичное время ответа

| Тип запроса | Время |
|-------------|-------|
| Простой SQL запрос | 15-30 сек |
| Сложный анализ | 30-60 сек |
| Контекстный диалог | 30-90 сек |
| Таймаут | 300 сек (5 мин) |

### Ограничения ресурсов

- **CPU контейнера:** 1 ядро
- **RAM контейнера:** 1GB
- **Размер БД:** 89MB (141,655 записей)

## 🔍 Отладка

### Проверка Docker контейнера

```bash
# Статус контейнера
docker ps | grep claude-sandbox

# Логи контейнера
docker logs claude-sandbox

# Вход в контейнер
docker exec -it claude-sandbox /bin/bash

# Проверка Claude CLI
docker exec claude-sandbox claude --version
```

### Проверка БД в контейнере

```bash
# Проверка файла БД
docker exec claude-sandbox ls -lh /workspace/telegram_messages.db

# Тестовый запрос к Claude
docker exec claude-sandbox claude --print --dangerously-skip-permissions \
  "Используй telegram_messages.db. Сколько всего записей?"
```

### Проверка логов бота

```bash
# Запуск с повышенным уровнем логирования
# В bot_secure.py изменить:
logging.basicConfig(level=logging.DEBUG)
```

## 🛡️ Сравнение версий

| Функция | bot.py | bot_advanced.py | bot_secure.py |
|---------|--------|-----------------|---------------|
| Контекстные диалоги | ❌ | ✅ | ✅ |
| Docker изоляция | ❌ | ❌ | ✅ |
| Read-only БД | ❌ | ❌ | ✅ |
| Ограничение ресурсов | ❌ | ❌ | ✅ |
| Защита от чтения .env | ❌ | ❌ | ✅ |
| Таймауты | ❌ | ❌ | ✅ |
| Markdown fallback | ❌ | ✅ | ✅ |

## 📚 Связанные файлы

- `Dockerfile.claude-sandbox` - Docker образ с Claude CLI
- `docker-compose.yml` - Конфигурация контейнера
- `SECURITY_SETUP.md` - Инструкция по безопасной настройке
- `telegram_messages.db` - База данных с сообщениями
- `.env` - Переменные окружения

## 🎯 Выводы

**bot_secure.py** реализует многоуровневую защиту:

1. **Изоляция процессов** - Claude CLI в Docker контейнере
2. **Принцип наименьших привилегий** - непривилегированный пользователь
3. **Read-only данные** - БД доступна только для чтения
4. **Ограничение ресурсов** - защита от DoS
5. **Graceful degradation** - fallback на plain text при ошибках
6. **Контекстные диалоги** - улучшенный UX

Это production-ready решение для безопасной работы с AI-агентами в продакшене.
