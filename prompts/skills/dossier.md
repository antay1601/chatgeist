# Skill: Досье на пользователя

## Триггеры

Этот skill активируется при запросах вида:
- "досье на @username"
- "расскажи о пользователе X"
- "кто такой X"
- "профиль X"
- "информация о X"

## Инструкции

Создай подробное досье на указанного пользователя на основе его активности в чате.

### Структура досье

📋 ДОСЬЕ: [Имя пользователя]
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

👤 Основная информация
   Username: @...
   Display name: ...
   ID: ...

📊 Статистика активности
   Всего сообщений: X
   Первое сообщение: дата
   Последнее сообщение: дата
   Период активности: X дней/месяцев/лет
   Среднее сообщений в день: X

⏰ Паттерны активности
   Самые активные дни недели: ...
   Самые активные часы: ...
   Пик активности: ...

💬 Контент-анализ
   Средняя длина сообщения: X символов
   Часто используемые слова/темы: ...
   Стиль общения: (формальный/неформальный/технический/...)

🔥 Топ сообщений (по реакциям)
   1. "текст..." (X реакций) — ссылка
   2. ...
   3. ...

💭 Взаимодействия
   Чаще всего отвечает на: @...
   Чаще всего получает ответы от: @...

### SQL-запросы для досье

```sql
-- Основная статистика пользователя
SELECT
    sender_username,
    sender_display_name,
    sender_id,
    COUNT(*) as total_messages,
    MIN(date_iso) as first_message,
    MAX(date_iso) as last_message,
    AVG(LENGTH(message)) as avg_message_length
FROM messages
WHERE sender_username = 'target_username'
   OR sender_display_name LIKE '%target_name%';

-- Активность по дням недели
SELECT
    strftime('%w', date_iso) as day_of_week,
    COUNT(*) as messages
FROM messages
WHERE sender_username = 'target_username'
GROUP BY day_of_week
ORDER BY messages DESC;

-- Активность по часам
SELECT
    strftime('%H', date_iso) as hour,
    COUNT(*) as messages
FROM messages
WHERE sender_username = 'target_username'
GROUP BY hour
ORDER BY messages DESC;

-- Топ сообщений по реакциям
SELECT message, reactions_count, permalink, date_iso
FROM messages
WHERE sender_username = 'target_username'
ORDER BY reactions_count DESC
LIMIT 5;

-- С кем взаимодействует (отвечает)
SELECT
    m2.sender_display_name as replies_to,
    COUNT(*) as count
FROM messages m1
JOIN messages m2 ON m1.reply_to_msg_id = m2.id
WHERE m1.sender_username = 'target_username'
GROUP BY m2.sender_id
ORDER BY count DESC
LIMIT 5;
```

### Дополнительные указания

- Если пользователь не найден по username, попробуй поиск по display_name
- Если данных мало (< 10 сообщений), укажи это и дай краткую сводку
- Добавь интересные факты, если они есть (например, самое длинное сообщение)
- Не раскрывай приватную информацию сверх того, что есть в публичных сообщениях
