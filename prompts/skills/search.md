# Skill: Поиск по сообщениям

## Триггеры

Этот skill активируется при запросах вида:
- "найди сообщения про X"
- "поиск: X"
- "где упоминается X"
- "кто писал про X"

## Инструкции

Выполни полнотекстовый поиск и представь результаты в удобном виде.

### Структура ответа

🔍 ПОИСК: "[запрос]"
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📊 Найдено: X сообщений

📝 Результаты (топ по релевантности/дате):

1. 📅 [дата] | 👤 @username
   💬 "текст сообщения..."
   🔗 ссылка

2. ...

### SQL для поиска

```sql
-- Полнотекстовый поиск (FTS5)
SELECT
    m.date_iso,
    m.sender_username,
    m.sender_display_name,
    m.message,
    m.permalink,
    m.reactions_count
FROM messages m
JOIN messages_fts fts ON m.id = fts.rowid
WHERE messages_fts MATCH 'поисковый запрос'
ORDER BY m.date_iso DESC
LIMIT 20;

-- Поиск по LIKE (если FTS не работает)
SELECT * FROM messages
WHERE message LIKE '%ключевое слово%'
ORDER BY date_iso DESC
LIMIT 20;
```

### Дополнительные указания

- Показывай контекст сообщения (обрезай длинные до ~200 символов)
- Группируй по авторам если много результатов от одного человека
- Указывай общее количество найденных сообщений
- Предлагай уточнить запрос если результатов слишком много (> 100)
