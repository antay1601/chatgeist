# Skill: Топы и рейтинги

## Триггеры

Этот skill активируется при запросах вида:
- "топ участников"
- "самые активные"
- "рейтинг авторов"
- "кто больше всех пишет"
- "популярные сообщения"
- "топ по реакциям"

## Инструкции

Создай рейтинг на основе запрошенной метрики.

### Варианты топов

📊 ТОП УЧАСТНИКОВ (по сообщениям)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🥇 @username — X сообщений (Y%)
🥈 @username — X сообщений (Y%)
🥉 @username — X сообщений (Y%)
4. @username — X сообщений
5. ...

---

🔥 ТОП СООБЩЕНИЙ (по реакциям)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. 💬 "текст..."
   👤 @author | 📅 дата | ❤️ X реакций
   🔗 ссылка

2. ...

---

📈 ТОП ПО ПРОСМОТРАМ
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. 👁 X просмотров
   💬 "текст..."
   🔗 ссылка

### SQL-запросы

```sql
-- Топ авторов по количеству сообщений
SELECT
    sender_display_name,
    sender_username,
    COUNT(*) as messages,
    ROUND(COUNT(*) * 100.0 / (SELECT COUNT(*) FROM messages), 1) as percent
FROM messages
GROUP BY sender_id
ORDER BY messages DESC
LIMIT 10;

-- Топ по реакциям
SELECT
    message,
    sender_display_name,
    date_iso,
    reactions_count,
    reactions_detail,
    permalink
FROM messages
WHERE reactions_count > 0
ORDER BY reactions_count DESC
LIMIT 10;

-- Топ по просмотрам
SELECT message, views, permalink, date_iso
FROM messages
WHERE views > 0
ORDER BY views DESC
LIMIT 10;

-- Топ по пересылкам
SELECT message, forwards, permalink, date_iso
FROM messages
WHERE forwards > 0
ORDER BY forwards DESC
LIMIT 10;
```

### Дополнительные указания

- Добавляй проценты для контекста
- Используй медали для топ-3
- Указывай временной период если запрошено
- Для авторов показывай username и display_name
