# Подробное руководство: Добавление чатов и скачивание истории

## Содержание

1. [Добавление чатов для мониторинга](#1-добавление-чатов-для-мониторинга)
2. [Скачивание истории чатов](#2-скачивание-истории-чатов)

---

## 1. Добавление чатов для мониторинга

### Обзор

Система хранит список чатов для мониторинга в файле `targets.json`. Каждый чат имеет:
- **alias** — короткое имя (используется для имени файла БД)
- **target** — идентификатор чата в Telegram

### Структура targets.json

```json
{
  "chats": {
    "durov": "durov",
    "python_ru": "python_ru",
    "my_group": -1001234567890
  },
  "updated_at": "2025-12-16T20:38:20.179947"
}
```

### Форматы target (идентификатора чата)

| Формат | Пример | Когда использовать |
|--------|--------|-------------------|
| Username без @ | `durov` | Публичные каналы/группы |
| Username с @ | `@durov` | Публичные каналы/группы |
| Ссылка t.me | `t.me/durov` | Любые публичные чаты |
| Числовой ID | `-1001234567890` | Приватные группы/каналы |

### Способ 1: Командная строка (рекомендуется)

```bash
# Добавить публичный канал
python update_manager.py --add durov @durov

# Добавить по ссылке
python update_manager.py --add news t.me/bbcrussian

# Добавить приватную группу по ID
python update_manager.py --add mygroup -1001234567890
```

**Что происходит при выполнении:**
1. Загружается текущий `targets.json` (или создаётся новый)
2. Добавляется запись `"alias": "target"`
3. Файл сохраняется с обновлённой меткой времени

### Способ 2: Редактирование targets.json вручную

```bash
nano targets.json
```

```json
{
  "chats": {
    "durov": "durov",
    "python": "python_ru",
    "news": "bbcrussian",
    "private": -1001234567890
  }
}
```

### Как узнать ID приватного чата

Если чат приватный (нет публичного username), нужен числовой ID:

```bash
python -c "
from telethon.sync import TelegramClient
import os
from dotenv import load_dotenv
load_dotenv()
client = TelegramClient('temp', int(os.getenv('TELEGRAM_API_ID')), os.getenv('TELEGRAM_API_HASH'))
client.start()
for dialog in client.iter_dialogs():
    print(f'{dialog.id}: {dialog.name}')
"
```

Скрипт выведет список всех ваших чатов с ID.

### Управление списком чатов

```bash
# Просмотр списка
python update_manager.py --list

# Вывод:
# 📋 Список чатов для мониторинга:
#    durov: durov (5000 сообщений)
#    python: python_ru (нет БД)

# Удаление чата
python update_manager.py --remove durov
```

При удалении автоматически удаляются:
- Запись из `targets.json`
- Файл БД `databases/durov.db`
- Папка экспорта `export_data/durov/`

### Правила именования alias

| Правильно | Неправильно | Почему |
|-----------|-------------|--------|
| `durov` | `Дуров` | Избегайте кириллицы |
| `python_ru` | `python ru` | Без пробелов |
| `news2024` | `news-2024` | Без дефисов (лучше) |
| `mygroup` | `my.group` | Без точек |

Alias становится именем файла: `databases/{alias}.db`

---

## 2. Скачивание истории чатов

### Обзор процесса

```
┌─────────────────┐     ┌──────────────────┐     ┌─────────────────┐
│  Telegram API   │────▶│  tg_dump_with_   │────▶│  messages.jsonl │
│  (Telethon)     │     │  reactions.py    │     │                 │
└─────────────────┘     └──────────────────┘     └────────┬────────┘
                                                          │
                                                          ▼
                        ┌──────────────────┐     ┌─────────────────┐
                        │  jsonl_to_       │────▶│  {alias}.db     │
                        │  sqlite.py       │     │  (SQLite)       │
                        └──────────────────┘     └─────────────────┘
```

### Авторизация (первый запуск)

При первом запуске Telethon запросит авторизацию:

```
Please enter your phone (or bot token): +79001234567
Please enter the code you received: 12345
```

После успешной авторизации создаётся файл сессии `update_manager_session.session`.
При последующих запусках авторизация не требуется.

### Инкрементальное vs полное обновление

**Инкрементальное обновление (по умолчанию):**
- Определяется `last_id` — максимальный ID сообщения в существующей БД
- Скачиваются только сообщения с `id > last_id`
- Быстро, экономит трафик и время

**Полное обновление (`--full`):**
- Игнорирует `last_id`, скачивает всё с начала
- Нужно при повреждении БД или изменении структуры
- Занимает много времени для больших чатов

### Команды скачивания

```bash
# Обновить все чаты из targets.json
python update_manager.py

# Обновить конкретный чат
python update_manager.py --chat durov

# Ограничить количество (для теста)
python update_manager.py --chat durov --limit 1000

# Полный дамп (игнорировать историю)
python update_manager.py --chat durov --full

# Комбинация: полный дамп с лимитом
python update_manager.py --chat durov --full --limit 5000
```

### Что происходит при скачивании

1. **Загрузка конфигурации**
   - Читается `targets.json`
   - Проверяются credentials (API_ID, API_HASH)

2. **Для каждого чата:**

   a. **Определение min_id:**
   ```python
   min_id = get_db_last_id(db_path)  # MAX(id) из существующей БД
   # Если БД нет или --full, min_id = 0
   ```

   b. **Скачивание сообщений (tg_dump_with_reactions.py):**
   - Подключение к Telegram через Telethon
   - Итерация по сообщениям с `id > min_id`
   - Для каждого сообщения извлекаются:
     - id, текст, дата
     - отправитель (id, username, display_name)
     - реакции (количество, детализация)
     - просмотры, пересылки
     - reply_to_msg_id (для треда)
   - Запись в `export_data/{alias}/messages.jsonl`

   c. **Конвертация в SQLite (jsonl_to_sqlite.py):**
   - Парсинг JSONL файла
   - Создание/обновление таблицы `messages`
   - Создание FTS индекса для полнотекстового поиска
   - Создание обычных индексов (date, sender, reactions)
   - Результат: `databases/{alias}.db`

3. **Вывод статистики:**
   ```
   📊 durov: +150 сообщений, всего в БД: 5150
   ```

### Структура файлов после скачивания

```
chatgeist/
├── export_data/
│   ├── durov/
│   │   └── messages.jsonl      # Сырой дамп (JSONL)
│   └── python_ru/
│       └── messages.jsonl
│
├── databases/
│   ├── durov.db                # SQLite для бота
│   └── python_ru.db
│
└── update_manager_session.session  # Сессия Telethon
```

### Формат JSONL (промежуточный)

Каждая строка — JSON-объект:

```json
{
  "id": 12345,
  "timestamp": 1702742400,
  "date": "2024-12-16T20:00:00+00:00",
  "message": "Текст сообщения",
  "sender_id": 123456789,
  "sender_username": "durov",
  "sender_display_name": "Pavel Durov",
  "reply_to_msg_id": null,
  "reactions_count": 150,
  "reactions": {"👍": 100, "❤️": 50},
  "views": 50000,
  "forwards": 1200,
  "permalink": "https://t.me/durov/12345"
}
```

### Схема SQLite (финальная)

```sql
CREATE TABLE messages (
    id INTEGER PRIMARY KEY,
    timestamp INTEGER,
    date_iso TEXT,
    message TEXT,
    sender_id INTEGER,
    sender_username TEXT,
    sender_display_name TEXT,
    reply_to_msg_id INTEGER,
    reactions_count INTEGER DEFAULT 0,
    reactions_detail TEXT,      -- JSON строка
    views INTEGER,
    forwards INTEGER,
    permalink TEXT,
    media_path TEXT,
    is_service INTEGER DEFAULT 0
);

-- Индексы
CREATE INDEX idx_date ON messages(date_iso);
CREATE INDEX idx_sender ON messages(sender_username);
CREATE INDEX idx_reactions ON messages(reactions_count);

-- Полнотекстовый поиск (FTS5)
CREATE VIRTUAL TABLE messages_fts USING fts5(
    message,
    sender_display_name,
    content='messages',
    content_rowid='id'
);
```

### Просмотр статистики

```bash
python update_manager.py --stats

# Вывод:
# 📊 Статистика баз данных в databases/
#
#    durov.db:
#       Сообщений: 5150
#       Last ID: 98765
#       Период: 2015-08-10 — 2024-12-16
#
#    python_ru.db:
#       Сообщений: 45000
#       Last ID: 123456
#       Период: 2018-01-15 — 2024-12-16
```

### Обработка ошибок

**FloodWaitError:**
```
Telegram rate limit. Waiting 300 seconds...
```
Скрипт автоматически ждёт. Для больших чатов используйте `--limit`.

**ChatNotFoundError:**
- Проверьте, что вы состоите в чате
- Для приватных чатов используйте числовой ID

**SessionError:**
- Удалите файл сессии и авторизуйтесь заново:
```bash
rm update_manager_session.session
python update_manager.py --chat durov
```

### Автоматическое обновление (Cron)

```bash
# Редактировать crontab
crontab -e

# Каждый час
0 * * * * cd /path/to/chatgeist && python update_manager.py >> logs/update.log 2>&1

# Каждые 6 часов
0 */6 * * * cd /path/to/chatgeist && python update_manager.py >> logs/update.log 2>&1

# Каждый день в 3:00
0 3 * * * cd /path/to/chatgeist && python update_manager.py >> logs/update.log 2>&1
```

### Рекомендации

1. **Для больших чатов (100k+ сообщений):**
   - Первый дамп делайте с `--limit 10000` для теста
   - Затем полный дамп без лимита (может занять часы)

2. **Регулярное обновление:**
   - Настройте cron для инкрементального обновления
   - Каждый час — оптимально для активных чатов

3. **Перед деплоем:**
   - Убедитесь, что все нужные чаты добавлены в `targets.json`
   - Скачайте историю локально, затем скопируйте `databases/` на сервер

4. **Экономия места:**
   - Папку `export_data/` можно удалить после конвертации
   - Нужна только папка `databases/` для работы бота
